--- a/vendor/nim-codex/vendor/nim-bearssl/bearssl/csources/conf/Unix.mk
+++ b/vendor/nim-codex/vendor/nim-bearssl/bearssl/csources/conf/Unix.mk.patched
@@ -38,11 +38,26 @@ MKDIR = mkdir -p
 
 # C compiler and flags.
 CC = cc
+# Respect cross-compilation environment variables
+CC ?= cc
+CXX ?= c++
+AR ?= ar
+
+# Android cross-compilation support
+ifdef CC_aarch64-linux-android
+  CC = $(CC_aarch64-linux-android)
+endif
+ifdef CXX_aarch64-linux-android
+  CXX = $(CXX_aarch64-linux-android)
+endif
+ifdef AR_aarch64-linux-android
+  AR = $(AR_aarch64-linux-android)
+endif
+
 CFLAGS = -W -Wall -Os -fPIC
 CCOUT = -c -o 
 
 # Static library building tool.
-AR = ar
 ARFLAGS = -rcs
 AROUT =
 
